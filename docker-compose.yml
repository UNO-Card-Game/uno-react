

services:
  uno-react:
    build:
      context: . # Path to the React project directory
      dockerfile: Dockerfile # Dockerfile inside uno-react directory
    container_name: uno-react
    ports:
      - "3000:80" # Map port 80 inside the container to port 3000 on the host
    restart: always # Restart the container automatically if it crashes
    
